// Generates random mock data for testing

import type { UserData } from "../../src/lib/db";

// TODO: REWRITE FOR SONG SPOTLIGHT

export const mockData: UserData = [];

console.log(JSON.stringify(mockData));
